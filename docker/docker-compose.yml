

services:
  backend:
    build:
      context: ..
      dockerfile: docker/backend.Dockerfile
    container_name: bupt-ai-backend
    environment:
      - DEBUG=False
      - COZE_API_KEY=${COZE_API_KEY}
      - COZE_API_URL=${COZE_API_URL}
      - COZE_BOT_ID=${COZE_BOT_ID}
      - COZE_USER_ID=${COZE_USER_ID}
      - SECRET_KEY=${SECRET_KEY}
    env_file:
      - .env
    networks:
      - bupt-ai-network

  frontend:
    build:
      context: ..
      dockerfile: docker/frontend.Dockerfile
    container_name: bupt-ai-frontend
    depends_on:
      - backend
    networks:
      - bupt-ai-network

  nginx:
    build:
      context: ..
      dockerfile: docker/nginx.Dockerfile
    container_name: bupt-ai-nginx
    ports:
      - "80:80"
    depends_on:
      - backend
      - frontend
    networks:
      - bupt-ai-network

networks:
  bupt-ai-network:
    driver: bridge 
